server:
  port: 8083

spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:h2:file:./loandb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
